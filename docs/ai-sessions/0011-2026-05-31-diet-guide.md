# 0011 — Dedicated diets section with explanations (#51)

Date: 2026-05-31
Branch: `feat/51-diet-guide` → PR #86

## Goal
Issue #51 (P11): give users a dedicated, informational section that explains each
diet type, reachable from the profile.

## Changes
- `Domain/ValueObjects/DietEnums.swift`: added `DietType.explanation` — a short,
  informational description for all 10 diet types (principles, foods typically
  included/excluded). No medical claims. Localized via `String(localized:)`.
- `Features/Profile/DietGuideView.swift` (new): a `List` with a top disclaimer
  ("Informational only — not medical or nutritional advice…") and one section per
  diet showing its explanation, with stable accessibility identifiers.
- `Features/Profile/ProfileView.swift`: a `NavigationLink` "About these diets" in
  the Diet section opening `DietGuideView`.
- en/it `Localizable.strings`: new keys for the guide title, link label, disclaimer
  and the 10 diet explanations.
- `FoodMapTests/DietTypeTests.swift` (new): every diet has a non-empty explanation
  and explanations are distinct.

## Gate
- xcodegen ✓
- swiftformat ✓
- swiftlint --strict — 0 violations ✓
- xcodebuild test (parallel testing, 2 concurrent destinations) — **TEST SUCCEEDED** EXIT=0 ✓
